VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Local/admin only — used by scripts/seed.mjs to bypass RLS. Never prefix with
# VITE_ (that would bundle it into frontend code) and never commit the real value.
SUPABASE_SERVICE_ROLE_KEY=
